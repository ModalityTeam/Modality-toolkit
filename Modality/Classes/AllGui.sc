/*
	for all globals a-z, 
	proxyspace p, 
	all proxyspaces, 
	all Pdefs, 
	all Pdefns, 
	all Tdefs, 
	BufEnvir? 
	
	* show how many in use, 
	* button to open each gui 
		(with good initial positions and sizes)
		(numbox for how many?)
		(variants for small?)
	
	what else?
	
*/
AllGui : JITGui { 
	
}