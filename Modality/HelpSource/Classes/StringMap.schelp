TITLE:: StringMap
summary:: Simple Class to map an Array of strings to a value range between 0 and 1.
categories:: Control, Spec, Libraries/Modality
related:: Tutorials/How_to_create_a_description_file_for_OSC

DESCRIPTION::

StringMap maps a string from an array of possible strings onto a value range between 0 and 1.

Simple example:

code::
~map = StringMap.new( ["off","amber","red"] );

~map.map( 0 );
~map.map( 0.5 );
~map.map( 1 );


~map.unmap( "off" );
~map.unmap( "amber" );
~map.unmap( "red" );

~map.unmap( "orange" );

~map.default;
~map.keys;

~map.spec;
::

CLASSMETHODS::


METHOD:: new
Create a new StringMap.

ARGUMENT:: keys
An Array of strings to map onto.


INSTANCEMETHODS::

private:: init


METHOD:: map
Map a SimpleNumber onto the keys.

ARGUMENT:: inval
A SimpleNumber

returns:: a String

METHOD:: unmap
Unmap a String to a number:

ARGUMENT:: inval
a String

returns:: A SimpleNumber or nil if the string was not found.

METHOD:: default
The default value of the StringMap is 0;


METHOD:: keys
The Array of keys.

METHOD:: spec
The ControlSpec for the StringMap.


METHOD:: asSpec
Convenience method.

returns:: the StringMap